def critic_agent(state):
    prompt = f"""
You are a senior creative director reviewing AI-generated prompts.

Score the prompt from 1-10:
- composition quality
- luxury feeling
- advertising level

Return JSON:
{{
  "score": int,
  "feedback": string
}}

Prompt:
{state['prompt']}
"""
    res = llm.invoke(prompt)
    return json.loads(res.content)
